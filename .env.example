# Copia para .env apenas se for usar docker-compose com overrides.
# As variaveis efetivas do backend ficam em backend/.env (veja backend/.env.example).
POSTGRES_USER=escala
POSTGRES_PASSWORD=escala
POSTGRES_DB=escala_gru
JWT_SECRET=troque-este-segredo-em-producao
ADMIN_EMAIL=admin@escala.local
ADMIN_PASSWORD=admin123
